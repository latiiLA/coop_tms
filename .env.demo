# Copy to .env.local for a local portfolio demo (no API/DB required):
#   copy .env.demo .env.local
# Then: npm start
#
# On Vercel, set the same variable in Project Settings → Environment Variables.

REACT_APP_DEMO_MODE=true
